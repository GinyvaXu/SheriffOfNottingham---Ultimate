# Python 极简版实现计划书（《诺丁汉警长》联机）

> 版本：v0.2（决策已确认）｜日期：2026-08-01｜状态：已定稿，开始实现
> 定位：总企画书（00_）的落地执行计划。原则：**只实现功能，一切从简**。

---

## 1. 目标

用 Python 做一个能玩的《诺丁汉警长》极简版：
- 2–5 人联机对局（局域网 / 公网），**人数可调：有多少人开多少人的局**
- 完整实现经典规则五阶段（市场 → 装袋 → 申报 → 检查 → 回合结束）
- 文字聊天（线上嘴炮的底线功能）
- 房主开房只开放本地端口；公网联机由玩家自行配置端口映射/穿透工具
- 不做任何多余系统：无账号、无成就、无皮肤、无动画（有基本反馈即可）

---

## 2. 已确认决策

| # | 决策点 | 决定 |
|---|---|---|
| D1 | GUI 框架 | **pygame**（已安装 2.6.1） |
| D2 | 界面形式 | **纯按钮 + 文字**，不做卡牌图形 |
| D3 | 联机穿透 | **不开 UPnP 自动映射**；房主仅监听本地端口并显示连接信息，外部玩家自行做端口映射（路由器转发 / frp / ngrok 等） |
| D4 | 房主身份 | **房主即服务器且参玩**（信任模式，不防作弊） |
| D5 | AI 补位 | **不做 AI**；人数可调，到几个人开几人的局（2–5） |
| D6 | 断线处理 | **断线等待重连，不托管**；重连后继续 |
| D7 | 规则范围 | **只做经典规则**（无皇家货物、无 2 人特殊规则） |
| D8 | 代码组织 | **模块化拆文件**，便于后期调整 |

---

## 3. 代码结构

```
项目目录/
├─ game.py     规则与状态机（纯逻辑，无 UI 无网络，可单测）
├─ net.py      TCP 服务器 + JSON 协议 + 房主/客户端连接管理
├─ gui.py      pygame 界面（主菜单 / 大厅 / 对局 / 结算）
├─ main.py     入口：--host 建房  /  --join ip:port 加入
└─ test_bot.py 自动化测试：机器人玩家跑完整局验证规则与联机
```

- `game.py`：牌堆、手牌、摊位、袋子、五阶段状态机、终局计分。
- `net.py`：房主监听端口（默认 5555）；换行分隔 JSON 消息；服务器权威判定。
- `gui.py`：纯按钮界面：主菜单（建房/加入）、大厅（人数、开始）、对局（手牌/袋子/申报/检查/贿赂/聊天）。
- `test_bot.py`：无界面机器人，用于自动验证「整局可玩通、结算正确」。

---

## 4. 联机方案（D3）

1. 房主启动时监听本地端口（默认 5555，可用 --port 修改）。
2. 程序显示给房主：本机局域网 IP、公网 IP 查询结果、提示文案。
3. **不做自动穿透**：公网联机时由房主/玩家自行完成端口映射，任选其一：
   - 路由器端口转发（把公网端口转发到房主电脑的 5555）；
   - 穿透工具（frp / ngrok / 花生壳等）把公网端口映射到本机 5555；
   - 局域网直接连局域网 IP。
4. 加入房间只需填 `IP:端口`。

**网络拓扑**：房主 = 权威服务器 + 玩家，星型直连；消息明文、信任模式。

---

## 5. 规则实现范围（经典规则全集）

- 开局：每人 50 金币 + 6 张手牌；两个弃牌堆各 5 张公开；随机首任警长。
- 市场：每人最多弃 5 张、抽回 6 张（可先抽弃牌堆顶、后抽牌堆；弃牌统一放入弃牌堆1，简化）。
- 装袋：同时秘密放 1–5 张（服务器保管，其他人不可见）。
- 申报：类型（合法四选一）+ 张数；张数强制 = 袋内张数。
- 检查：警长逐袋决定放行/检查；检查前袋主可**贿赂金币+留言**（简化版，不支持货物/承诺系统校验）。
  - 放行：合法货公开上摊、走私品面朝下保密（只公开数量）；贿赂归警长。
  - 检查属实：警长按卡面罚金（=价值）付给商人；货物上摊。
  - 检查撒谎：如实申报的部分上摊；其余没收弃牌；按没收卡罚金罚款。
  - 金币不足：用摊位合法货抵债（不找零）→ 走私货 → 剩余债务免除。
- 结算：货物分 + 金币分 + 国王/王后加成（苹果20/10、奶酪15/10、面包15/10、鸡10/5，含平局规则）。
- 局数：每人当警长 2 次（3 人局 3 次）。
- 断线：等待重连（同名重连即可恢复原位），期间对局暂停等待。

---

## 6. 界面（极简）

- 主菜单：昵称输入、创建房间（人数上限 2–5）、加入房间（IP:端口）、退出。
- 大厅：已加入玩家列表、人数提示、房主「开始游戏」按钮（≥2 人即可开始）。
- 对局界面（1280×800）：
  - 顶部横幅：阶段 / 第几回合 / 现任警长
  - 玩家面板：昵称、金币、手牌数、摊位（各合法货数量+走私数量）、申报内容、断线状态
  - 下方：自己的手牌（按钮点选）、袋子张数、按阶段出现的操作按钮
  - 右侧：聊天记录 + 输入框 + 快捷喊话（“别查我！”“全是苹果！”等）
- 结算页：名次、总分、金币分、货物分、国王/王后加成。

---

## 7. 里程碑

| 阶段 | 内容 | 验证方式 |
|---|---|---|
| M1 | game.py 状态机 | 命令行模拟完整局，结算正确 |
| M2 | net.py 联机框架 | 本机多开互通 |
| M3 | gui.py + main.py | 本机双开玩通一局 |
| M4 | 公网验证 | 两台不同网络机器联机成功（配合端口映射） |
| M5 | 收尾：说明文档 | 内测一局无致命 bug |

---

## 8. 风险与备注

- 公网直连受运营商 NAT 类型影响，失败时需中转工具（frp/ngrok）；代码不内置，仅给提示。
- 信任模式下房主可偷看袋/改状态，仅限朋友联机。
- pygame 文本输入不支持中文输入法组合输入（IME），聊天建议用英文+快捷喊话（中文）。
- 明文 TCP，无加密、无防作弊。
---

## 附：实现进度（2026-08-01）

- [x] M1 game.py 状态机（命令行模拟验证）
- [x] M2 net.py 联机框架（本机多开测试通过）
- [x] M3 gui.py + main.py（本机双开 GUI 冒烟通过）
- [x] M5 自动化测试（test_bot.py：2/3/4/5 人局全流程 + 断线重连通过）
- [ ] M4 公网实测（需两台不同网络机器配合端口映射验证）