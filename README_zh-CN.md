[English](README.md) | **中文**

# 诺丁汉警长 - Python 极简联机版

经典桌游《诺丁汉警长》的 Python 极简实现。功能完整、代码简洁、模块化设计。
经典规则外加两个可选模块（皇家赏赐卡与黑市任务），均可开关。
v1.1.0 新增**模组系统**与**可安装/可卸载的安装包**。
v1.2.0 新增**游戏图标**、对局装饰图案、**内置模组管理界面**与**赛博朋克皮肤模组**。
v1.2.1 新增**自动更新**：启动时自动检测 + 主菜单「检查更新」按钮，可一键下载并静默安装 GitHub Release 上的新版。
v1.2.2 修复**模组启用/禁用保存失败**（UTF-8 BOM 清单、只读文件）与**更新检查超时**（多源备份 + 重试 + 友好错误提示）。
v1.2.3 彻底修复**安装到 Program Files 后 mod.json 不可写**：安装包为 mods 目录开放写入权限；游戏在保存失败时自动修复目录 ACL（icacls）；若安装目录仍不可写，则自动改用 %APPDATA% 下的用户模组目录并迁移已有模组。模组管理界面会显示当前实际使用的模组目录路径。
v1.3.0 新增游戏内**模组市场**（浏览 GitHub 托管的模组目录、一键下载安装），内置 **5 款纯文本换皮模组**（赛博朋克 / 中世纪 / 星光殖民地 / 蒸汽朋克 / 奥术秘境），并让服务器**聊天消息跟随本地换皮**——换皮仅客户端生效，联机时每位玩家只能看到自己的版本。

## v1.4.0 新增**玩法模组**（改变游戏规则的模组）与**服务器校验**：房间内所有玩家必须安装相同的玩法模组才能开局，大厅显示每位玩家的模组状态，并支持一键下载安装缺失的玩法模组。内置两个示例玩法模组（马拉松集市、香料之路）。

## v1.4.1 新增**人物档案与头像系统**：姓名与头像保存在本地配置文件夹 `%APPDATA%/SheriffOfNottingham/profile.json`，每次启动自动恢复，无需重新上传；提供 **8 款内置头像**，也可**上传自己的图片**（自动压缩到标准尺寸并同步给房间内所有人）；头像显示在主菜单、大厅、玩家面板与结算界面。**5 款换皮模组新增配套主题头像配色**。同时修复**模组市场列表截断**（香料之路不再被隐藏）、模组启用/安装后出现**重启游戏按钮**、以及**更新重启报错**（批处理改为单 CRLF、等待旧进程完全退出）。新的玩法模组创意已写入 **MOD_IDEAS.md**（中英双语），供审核。
v1.6.6 新增四个**玩法模组**：**万能卡**（房主在开局前设置牌库中的万能卡数量；万能卡属于合法品，查验时袋中的万能卡自动变成所申报的货物）、**警长情报**（每回合警长有一次机会支付 n 元，n 等于剩余未查商人袋中卡片总数，以此获知未查商人中共有多少张违禁品，但只能知道 0-2、3-5 等区间，且待查玩家不少于两人时才能使用）、**超级违禁品**（每种违禁品增加一张超级违禁品，价值与罚金为该类违禁品的三倍）、**商人声望 v2**（合法货物与申报种类不符不再扣声望；声望为正时抽到合法品的概率梯度提升，声望 5 时每张牌 90% 概率为合法品；声望为负时违禁品概率提高但整体下调，留有余地）。所有内置模组版本与游戏版本同步为 1.6.6。
v1.6.7 新增**玩法模组「风云变幻事件包」**：每回合开始公开 1 张事件卡，本回合内生效，共 10 个中低难度事件——**丰收之年**（市场多摸 1 张）、**饥荒之年**（装袋上限 4 张）、**瘟疫蔓延**（随机一种合法货禁运，万能卡也不能变）、**集市日**（查实赔偿每张 +1 金）、**城门税**（封袋缴纳 1 金）、**官员视察**（警长本回合必须至少查 1 人）、**全城封锁**（违禁品罚款翻倍，含超级违禁品）、**恩赦日**（只没收不罚款）、**黑市行情**（成功走私的违禁品价值 +1）、**街头谣言**（警长每回合 1 次可偷看 1 张待查玩家的牌）。局内聊天栏上方常驻事件横幅，显示事件名称与完整效果；瘟疫时横幅列出禁运货物、装袋时禁运牌置灰不可选；警长检查界面新增「偷看」按钮；人机装袋遵守袋上限与禁运规则；黑市任务不受事件影响（已确认）。同时修复：大厅万能卡数量输入框可点击修改、大厅玩法模组行可展开查看完整中英规则、用万能卡申报时提示 N 张计为所申报货物、局内聊天记录支持真实滚动条翻看且不再因空记录崩溃。所有内置模组版本与游戏版本同步为 1.6.7。
v1.7.0 新增**切屏过渡动画**、**可缩放窗口**（整个界面以固定 1280x800 逻辑画布渲染并等比缩放到真实窗口，5-6 人时玩家面板自动紧凑化，缓解六人局拥挤）、**设置页**（窗口预设 1280x800/1600x900/1920x1080/2560x1440、自定义宽高、全屏与无边框模式）、**历史战绩页**（每局自动记录时间、人数、回合与详细分数及排名，保存在 `%APPDATA%/SheriffOfNottingham/history.json`，可一键清空），以及首页**赞助按钮**（打开作者爱发电主页）。黑市任务面板重做：彩色货物芯片与星标奖励槽。所有内置模组版本与游戏版本同步为 1.7.0。
v1.7.1 让**更新检查并行化、更适应国内网络**：所有镜像源（GitHub 加速代理、raw GitHub、jsDelivr CDN 边缘节点、GitHub releases API）同时探测并共用一条全局时限，整个检查只需几秒，不再在国内网络上长时间卡住或显示"检查超时"。新增 jsDelivr CDN 源（cdn / fastly / gcore），国内快速稳定；多源同时返回时取版本号最高者，旧 CDN 缓存永远不会掩盖新版本。同时清理了 GitHub Release 列表（移除旧版 v1.5.3 草稿发布）。所有内置模组版本与游戏版本同步为 1.7.1。
v1.7.3 对局内界面进行整体重做：每个模块都有固定区域、互不重叠（顶部信息条、通栏玩家区、黑市任务面板、独立的我的桌面信息行、手牌区、提示行、操作区与独立聊天栏）。玩家名牌加宽、高度随摊位内容自适应，5-6 人局不再挤成一团；聊天区为玩家信息让位，快捷用语移入聊天面板底部不再被截断，牌堆图标移入顶部信息条，贿赂/还价输入框并入操作区。性能大幅提升：鼠标静止时直接复用缓存画面，2560x1440 下首页/更新页每帧从约 15.7ms 降至约 3.5ms（快约 4 倍），悬停/淡入/滚动/聊天仍即时刷新；更新日志换行与背景装饰改为缓存。更新清单历史已重写为干净的双语文本。所有内置模组版本与游戏版本同步为 1.7.3。
v1.7.4 修复 6 人局"Passed a NULL pointer"崩溃：玩家名牌的"缩小字体"循环其实在反向放大字号（像素行高大于字号），窄面板（5-6 人局）遇到较长名字时字号一路暴涨，直到 SDL_ttf 返回坏字体，下一次测量文本时游戏直接崩溃。现在名牌字体会正确缩小，超长名字自动以省略号截断。字体加载增加校验：get_font 每次加载后都会实测字体可用性，损坏/截断的字体文件（例如安装或更新中断产生）会被自动跳过，不再导致文字渲染崩溃。所有内置模组版本与游戏版本同步为 1.7.4。
v1.7.2 将设置页重做为清爽的游戏风格布局（左侧：分辨率预设 + 自定义尺寸；右侧：显示模式 窗口模式/无边框/全屏 与画面适配 适应/拉伸）。新增"拉伸填满"模式，窗口比例与游戏不一致时铺满整个窗口，不再出现黑边。画布改用双线性平滑缩放并缓存呈现面，大窗口预设（1920x1080/2560x1440）不再有像素块与毛边。切屏淡入动画改为缓存复用且更短，首页与更新页不再卡顿（2560x1440 下 90+ FPS）。按钮加入反馈动画：悬停颜色渐变、按下下沉 2px、点击柔光闪烁。黑市任务提交按钮已对齐到每个任务行右侧。所有内置模组版本与游戏版本同步为 1.7.2。
v1.6.9 让**国内网络也能自动更新**：更新检测与安装包下载不再依赖直连 GitHub，游戏会按顺序自动尝试多个国内可用的 GitHub 加速镜像（ghfast.top、gh-proxy.com、ghproxy.net、gh.llkk.cc）——更新清单与安装包下载都走同样的多源回退，即使 api/raw.githubusercontent.com 被屏蔽或限速也能正常检查与更新。高级用户可在 `%APPDATA%/SheriffOfNottingham/mirror.json` 配置自建镜像（例如 Gitee 仓库，`{"manifest": "...", "installer": "..."}`，两个字段都可选），配置后优先使用该镜像。模组市场下载同样接入国内加速镜像，并优先使用 GitHub 实时源，避免 jsDelivr 缓存滞后。更新失败界面现在会提示国内网络受限的原因，并引导手动打开更新页面下载。所有内置模组版本与游戏版本同步为 1.6.9。
v1.6.8 将「风云变幻事件包」扩充至 **20 个事件**（新增苹果歉收、奶酪盛典、零容忍、双倍赔偿、物资短缺、游园日、赏金榜、警长加薪、谣言升级、金库充盈），事件改为每回合随机抽取、**永不枯竭**且不会连续两回合重复；新增**六人局**（房间人数上限 2-6）；修复丰收之年真正能多摸 1 张；修复黑市任务在最后一轮仍能自动提交领取奖励；聊天消息按类型着色区分。所有内置模组版本与游戏版本同步为 1.6.8。


文件结构

```
game.py      规则与状态机（纯逻辑，不含界面/网络）
bot.py       人机 AI 模块（简单/普通/困难，运行在房主服务器上）
net.py       TCP 服务器 + JSON 协议 + 断线/重连 + 人机驱动
gui.py       pygame 纯按钮界面
gfx.py       程序化装饰图案（徽章/金币/卡背/图标）
mods.py      模组加载器（exe 旁的 mods/ 目录，ModAPI 支持添加/修改/换名）
profile.py   本地人物档案（姓名+头像，%APPDATA%/SheriffOfNottingham/profile.json）
market.py    模组市场：远程目录 + 一键下载安装
mods_market.json  模组市场目录（raw + jsDelivr CDN 双源）
mods_pack/   提供给游戏内市场的模组压缩包
lang.py      中英双语文本 + 模组卡牌名重建
main.py      入口（--version 弹出版本对话框）
version.py   __version__ = "1.7.0"
installer.iss  Inno Setup 安装脚本
test_bot.py  无头人机自动化测试（完整对局 + 断线重连）
test_ai_bots.py  大厅人机测试（房主添加机器人并完整对局）
test_new_rules.py  皇家赏赐卡 & 黑市任务单元测试
test_mods.py 模组加载器单元测试（注册/修改/启用/禁用/报错）
test_reskin_mods.py  换皮模组 + 模组市场安装测试
test_avatars.py  人物档案/头像存取 + 联机头像同步测试
test_back_lobby.py  结算返回房间 + 同难度人机命名测试
MOD_IDEAS.md  玩法模组创意提案（中英双语，供审核）
mods/        内置模组目录（README.md + example_mod + cyberpunk_mod，默认禁用）
```

## 依赖与安装

- Python 3.10+
- `pip install pygame`

## 语言

- 界面中英双语：默认中文，可切换英文。
- 菜单/大厅右上角按钮切换，或启动时加 `--lang en`。
- exe 内置中文字体（微软雅黑），无需系统字体即可正常显示中文。
  如需替换，放入 OFL 字体为 `assets\NotoSansCJKsc-Regular.otf`。
- 聊天/名字支持中文输入法（pygame 文本输入），也支持 Ctrl+V 粘贴。

## 快速开始

**开房（房主）：**
```
python main.py --host --players 4 --port 5555 --name ZhangSan
```
`--players` 为座位上限，2-5 人。房主在大厅点击"开始游戏"；游戏按实际加入人数
调整。大厅中房主可设置回合数（默认每人当两次警长）以及修改自己的名字。
大厅内所有人（房主、客人、人机）都必须点击"准备" 后房主才能开局；
加入的客户端必须与房主的游戏版本一致，版本不同会被房主拒绝并提示。

**加入：**
```
python main.py --join 192.168.1.5:5555 --name LiSi
```

**局域网**：直接连接房主局域网 IP。大厅中房主可点"复制地址"把 `LAN_IP:port`
复制到剪贴板发给好友，菜单里也有"粘贴"按钮可直接填入收到的 `IP:port`。
**互联网**：游戏不内置穿透，房主自行端口映射（二选一）：
- 路由器端口转发：把公网端口转发到房主机器的 5555。
- 穿透工具（frp / ngrok 等）：把公网端口映射到本地 5555。
- 然后把 `公网IP:端口` 发给好友加入。

## 人机（AI 机器人）

- 大厅中房主可添加简单/普通/困难三档机器人填补空位，开始前也可移除。
- 机器人在房主服务器上自主行动（市场弃摸牌、装袋、申报、贿赂、检查、黑市提交），
  不占用网络连接，决策只使用公开信息。
- 同难度的多个人机会自动编号命名（如 Bot-Easy 1、Bot-Easy 2），方便区分。
- 支持单人游玩：1 名真人 + 1-4 名机器人。

## 模组系统（v1.1.0 新增）

- 主菜单新增「模组」按钮，可打开模组管理界面：查看所有已装模组的名称/版本/说明，
  一键启用/禁用（写回各自 mod.json）、刷新列表，并显示加载错误。修改在重启游戏后生效。
- 内置 `cyberpunk_mod/`（默认禁用）：把全部货物名、阶段名与关键界面词改成霓虹赛博风并配霓虹配色。

`mods/` 目录（exe 旁；源码运行时为项目根目录）下，每个含 `mod.json` 的文件夹
就是一个模组：

```json
{
  "id": "example_mod",
  "name": "Example Mod",
  "version": "0.1.0",
  "description": "添加茶叶（违禁品）与梨（合法货物）。",
  "enabled": false
}
```

- `enabled: true` 即加载该模组；文件夹内可有 `mod.py`，其 `register(api)` 函数
  会在启动时被调用。
- `ModAPI` 支持添加卡牌类型或直接修改游戏本身：

```python
def register(api):
    api.add_contraband("TEA", "Tea", "茶叶", value=5, fine=3, cnt3=8, cnt6=12, color=(90, 160, 120))
    api.add_legal("PEAR", "Pear", "梨", value=3, fine=2, cnt3=24, cnt6=24,
                  king_bonus=10, queen_bonus=5, color=(140, 200, 90))
    api.patch("game", "HAND_SIZE", 7)     # 例如修改手牌上限规则
```

- `add_legal` / `add_contraband` / `add_royal` 注册新卡牌类型，可指定
  3 人局与 4-6 人局的卡牌数量（`cnt3`/`cnt6`）、颜色、价值、罚款，
  合法货物还可指定第一名/第二名终局奖励（`king_bonus`/`queen_bonus`）。
- `patch("game", 属性, 值)` / `get("game", 属性)` 可直接读写任意引擎属性（改规则）。
- 损坏的模组会被跳过并在菜单页显示错误，绝不会导致游戏崩溃。
- 完整 API 见 `mods/README.md`（中英双语）与示例 `mods/example_mod/`。
- 同一房间的玩家应安装相同的内容模组：服务器决定规则，客户端只需名称/颜色来渲染卡牌。

## ## 玩法模组（v1.4.0 新增）

玩法模组（mod.json 中 `"category": "rules"`）会改变游戏的实际规则，并携带**服务器校验**：

- 规则模组运行在**房主服务器**上（房主进程加载模组，服务器按修改后的规则驱动对局）。
- 房间内每名人类玩家必须安装**完全相同（id + 版本）**的玩法模组才能开始游戏。
- 大厅会显示房间要求的玩法模组与每位玩家的状态（一致 / 缺少）。缺少的玩家可点**“一键安装缺失玩法模组”**从内置模组市场下载安装，然后重启游戏重新加入。
- 机器人始终与房主一致（它们运行在服务器内）。
- 玩法模组新增的内容（如新卡牌）由服务器驱动；客户端因装有相同模组而能正确渲染。

### 内置示例玩法模组（默认禁用）

| 模组 | 作用 |
| --- | --- |
| 马拉松集市 | 延长对局：每名玩家 3 回合（3 人局 4 回合）。 |
| 香料之路 | 新增合法货物“胡椒”与违禁品“茶叶”，拓展贸易与走私。 |

### 推荐的玩法模组想法

| 模组想法 | 玩法改动 |
| --- | --- |
| 双倍罚款 | 所有检查罚款与赔偿翻倍，高风险高回报。 |
| 物价波动 | 每回合随机调整货物价值，市场充满变数。 |
| 黑市大亨 | 黑市任务奖励提高、需求张数改变。 |
| 严苛警长 | 警长每轮可多查一人，罚款加倍。 |
| 皇家盛宴 | 皇家卡数量翻倍、分值提高。 |
| 禁运令 | 随机一种货物整局禁用。 |
| 快节奏贸易 | 手牌上限 5、装袋 1-4 张。 |
| 慷慨国王 | 第一名/第二名奖励提高。 |
| 禁贿令 | 取消贿赂阶段，纯拼胆量。 |

安装包（v1.1.0）

`installer\SheriffOfNottingham-Setup-1.2.0.exe` 是 Inno Setup 制作的常规 Windows 安装程序：

- 安装游戏本体 + `mods\` 目录 + 桌面/开始菜单快捷方式。
- 可在系统"应用"中彻底卸载，会删除整个应用目录（含玩家自行添加的模组）。
- 支持中英文安装界面（安装时选择）。
- 重新构建：先运行 `打包.bat`，再执行 `ISCC.exe installer.iss`。

## 图标与装饰（v1.2.0）

- 程序化绘制的警长徽章图标：用于游戏窗口、exe 与安装包。
- 对局装饰：主菜单徽章 Logo、金币旁的硬币图标、警长名牌上的徽章、牌堆的卡背图案。

## 规则说明

- 市场：玩家可弃掉最多 5 张手牌，再从牌堆补回等量。弃牌堆保密，且不能再从中摸牌。
- 每种货物价值/罚款固定，卡牌数量随人数变化。
- 经典规则含 4 种合法货物（苹果/鸡肉/奶酱/面包）与 4 种违禁品（丝绸/弩/咖啡/酒），
  外加皇家赏赐卡。
- 黑市任务：某违禁品走私成功满 3 张自动提交，3 张卡弃掉，奖励
  （第一名 30-35 金币、第二名 25-30）全局通报，已完成的黑市任务其他人无法再做。

## 游戏流程

- **市场**：所有商人同时行动——每人选择弃掉 0-5 张手牌，然后从牌堆补回相同数量；全部人完成后才进入下一阶段。
- **装袋**：秘密挑选 1-5 张牌放入布袋，然后封袋。
- **申报**：所有商人同时申报（数量强制等于袋中张数，种类可以撒谎）。
- **检查**：袋主可先贿赂（金币+备注）；警长可以接受、放行、检查，也可以反还价索要更高金额——商人可再接受、拒绝或再次还价（每次谈判最多反还价 3 轮，之后只能接受/拒绝）。若商人拒绝警长的还价，仍然按第一次贿赂的出价生效（不再作废）。
- **聊天**：右下输入框，回车发送；另有快捷短语按钮。

## 结算后返回房间

- 对局结束（结算界面）点击**返回房间**即可让所有人回到同一房间的大厅
  （座位与人机保留，房主可开新一局），也可点"退出"离开游戏。

## 断线 / 重连

对局中断线会等待该玩家（不托管、不判负）。用**相同名字**重进同一房间即可恢复座位；
客户端也会自动重连。

## 自动化测试

```
python test_new_rules.py              # 皇家赏赐卡 + 黑市任务单元测试
python test_mods.py                   # 模组加载器测试
python test_back_lobby.py             # 结算返回房间 + 人机命名测试
python test_bot.py --players 3        # 3 人完整对局（含断线/重连）
python test_bot.py --players 5        # 5 人对局
python test_ai_bots.py --rounds 2     # 大厅人机测试
```

## 智能应用控制拦截（Windows 11）

全新 Windows 11 设备默认开启**智能应用控制（Smart App Control, SAC）**，
它会拦截「未签名或发布者不可信」的可执行程序。游戏目前未签名，因此安装后 SAC 可能提示：

> “智能应用控制已阻止可能不安全的应用 ... 无法验证其发布者”

玩家应急办法：
- 关闭该功能：Windows 安全中心 → 应用和浏览器控制 → 智能应用控制 → **关闭**。
  注意：关闭后在该电脑上是永久生效的（只能通过重置系统重新开启）。
- 若是浏览器下载的安装包，可先右键 exe → 属性 → 勾选「解除锁定」再运行。

开发者根治办法：用公开可信的代码签名证书（OV/EV 证书或 Microsoft Azure
Trusted Signing）同时签名游戏 exe 与安装包，然后执行：

```
.\sign_assets.ps1 -Thumbprint <证书SHA1>
```

详见 `sign_assets.ps1` 文件头的说明。

## 已知限制

- 信任模式：房主即服务器，理论上可作弊，仅适合熟人之间。
- 中英双语界面（默认中文；字体加载器保留系统中文字体兜底）。
- 明文 TCP，无加密与反作弊。
- 公网直连取决于 NAT 类型；失败可用 frp/ngrok 中转。
- 贿赂简化为金币+备注，不校验货物/承诺。

## 打包成 exe

```
打包.bat                        # 或手动执行下面命令
python -m PyInstaller --clean --noconfirm SheriffOfNottingham.spec
```

- 生成文件夹版 `dist\SheriffOfNottingham\`（exe + 常驻 `_internal\` 运行库，
  无需安装 Python，不再临时自解压），带 1.6.5 版本信息与内置 `mods\` 副本；
  运行时读取 exe **旁**的 `mods\` 目录，可自由增删模组。
- 房主首次运行会触发 Windows 防火墙提示，请允许以开放端口。
- 查看报错可改用 `--console` 打包，或直接运行 `python main.py`。
- exe 支持相同命令行参数：`SheriffOfNottingham.exe --host --players 4`
  或 `SheriffOfNottingham.exe --join 192.168.1.5:5555`。