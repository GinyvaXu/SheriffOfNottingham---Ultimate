{
  "version": "1.5.1",
  "url": "https://github.com/GinyvaXu/SheriffOfNottingham---Ultimate/releases/download/v1.5.1/SheriffOfNottingham-Setup-1.5.1.exe",
  "notes": "v1.5.1 - Fixed: \"Update & Restart\" did nothing (from v1.4)\nFix: an interrupted update used to leave a pending flag in %TEMP%\\SheriffUpdate; after that every click silently \"succeeded\" without installing. The flag now records the batch process id and is auto-cleared when stale, so updates always re-run.\nFix: the background update script now writes update.log / inno_install.log and, if the silent install still fails, opens the GitHub releases page as a fallback.\nImprovement: the installer now defaults to a per-user folder, so silent auto-updates never need an admin elevation prompt.\nGUI: home screen grouped panels, lobby sections, update changelog panel, 6 new rule mods + mod compatibility checks, bot personalities (from v1.5.0)."
}
